import * as Yup from 'yup';

class CartController {
  async index(req, res) {
    return res.status(200);
  }

  async store(req, res) {
    return res.status(200);
  }
}

export default new CartController();
